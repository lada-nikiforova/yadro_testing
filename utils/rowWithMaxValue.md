function rowWithMaxValue(data)
    rowSum(row)
        sum = 0
        for (r of row)
            max += r 

    maxRow = null

    for (row in data)
        max = 0
        sum = rowSum(row)
        if (sum > max)
            max = sum
            maxRow = row

    return maxRow