function exportToCSV(table):
    create file with name in write mode
    for each row in table:
        convert row to CSV string (через разделитель запятую)
        write the CSV string to the file
    close the file
