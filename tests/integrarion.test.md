```
test clickFetch
    вызывается API => данные появляются в таблице => выбирается строка с макс. значением

```
```
tect clickRow
    выбирается строка => выделяется в таблице => строится корректный график 
```
    
